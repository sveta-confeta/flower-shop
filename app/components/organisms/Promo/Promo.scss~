@use "../../../assets/scss/breakpoints" as *;
@use "../../../assets/scss/scale" as *;

.promo {
  width: 100%;
  height: 100vh;
  position: relative;
  padding-top: 76px;
  display: flex;
  justify-content: center;
  align-items: center;
  border-bottom-left-radius: 100px;
  border-bottom-right-radius: 100px;
  overflow: hidden;
  @media(max-width: $tablet) {
    padding-top: 30px;
    border-bottom-left-radius: 20px;
    border-bottom-right-radius: 20px;
    height: 100vh;
  }
  @media(min-width: $laptop) {
    border-bottom-left-radius: size-decstop(100);
    border-bottom-right-radius: size-decstop(100);
    padding-top: size-decstop(76);
    height: 100%;

  }

  &-picture {
    filter: brightness(0.4);
    position: absolute;
    top: 0;
    width: 100%;
    height: 100vh;
    object-fit: cover;
  }

  &__context {
    display: flex;
    flex-direction: column;
    align-items: center;
    position: relative;
    z-index: 1;

    &-text {
      font-size: 20px;
      line-height: 24px;
      font-weight: 400;
      margin-bottom: 24px;
      color: var(--title-all);
      @media(max-width: $tablet) {
        font-size: 16px;
        line-height: 19px;
      }
      @media(min-width: $laptop) {
        font-size: size-decstop(20);
        line-height: size-decstop(24);
        margin-bottom: size-decstop(24);
      }
    }

    &-title-wrap {
      margin-bottom: 70px;
      display: flex;
      flex-direction: column;
      align-items: center;
      @media(min-width: $laptop) {
        margin-bottom: size-decstop(70);
      }
    }

    &-title {
      font-size: 64px;
      color: var(--title-all);
      line-height: 77px;
      font-weight: 600;
      text-align: center;
      @media(max-width: 1183px) {
        max-width: 500px;
        font-size: 50px;
        line-height: 65px;
      }
      @media(max-width: $tablet) {
        max-width: 100%;
        font-size: 40px;
        line-height: 48px;
      }
      @media(min-width: $laptop) {
        font-size: size-decstop(64);
        line-height: size-decstop(77);
      }
    }

    &-underline-mob {
      display: inline-block;
      width: 244px;
      height: 22px;
      @media(min-width: $laptop) {
        width: size-decstop(244);
        height: size-decstop(22);
      }

    }
  }
}


